# MongoDB connection string (Atlas or local)
MONGODB_URI=

# Long random string used to sign the admin session cookie
# Generate one with: openssl rand -base64 32
AUTH_SECRET=

# Password for the /admin sign-in page
ADMIN_PASSWORD=
